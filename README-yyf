/* add int *bt_peer_id(int socket) - find peer id using socket */

/* after sending a data packet, call int wait_ack(int peer_id, unsigned seq) */

/* for each stream call unsigned get_timeout_seq(int peer_id) to 
 * determine retransmission */ 
 
/* TODO: control packet lost, similar to data packet */

/* use time() is inaccurate, should be milliseconds */

/* call int window_timeout(int peer_id) when a packet timeouts */

/* call get_cwnd_size(int peer_id) to get window size */
