/* begin interface */

/* call get_cwnd_size(int peer_id) to get window size */

/* call wait_ack(int peer_id, unsigned seq) after sending a data packet */

/* clean_download_timeout and clean_download_timeout return peer id */

/* call window_timeout(peer_id) if a packet timeouts */ 

/* end interface */



/* look at some TODOs in process_inbound_udp in peer.c, some send functions 
 * need implementation, like send_ACK. While ignore TODOs in keep_track and 
 * congestion_control; they just serves as a reminder */

/* add int *bt_peer_id(int socket) - find peer id using socket */
 
/* TODO: control packet lost, similar to data packet */

